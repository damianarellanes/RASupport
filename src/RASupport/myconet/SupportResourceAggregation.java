package RASupport.myconet;

/*import static thesis.Configuration.*;

public class SupportResourceAggregation {
	
	public static void printMessage(String msg) {
	
		System.out.println("------------------------------------------------------------");
		System.out.println(msg);
		System.out.println("------------------------------------------------------------");
	}
	
	public static void sendAdvertisementAgent(MycoNode np, MycoNode sp, int type) {
		
		if(type == ADVERTISEMENT_INITIAL) {
			printMessage(np + " sends an advertisement INITIAL agent to " + sp);
		}
		else if(type == ADVERTISEMENT_UPDATING) {
			printMessage(np + " sends an advertisement UPDTAING agent to " + sp);
		}
		else {
			printMessage("SupportResult.sendAvertisementAgent: non recognozible advertisement agent " + type);
		}		
	}	

}
*/